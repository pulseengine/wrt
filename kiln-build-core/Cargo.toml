[package]
name = "kiln-build-core"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true

[features]
default = ["std"]
std = ["anyhow/std", "clap/std", "kiln-runtime/std", "kiln-decoder/std"]
no_std = []

[dependencies]
# Core dependencies
anyhow = { workspace = true }
clap = { version = "4.5.60", features = ["derive", "env"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
walkdir = "2.5.0"
tempfile = "3.27"
fs_extra = "1.3.0"
chrono = { version = "0.4", features = ["serde"] }

# Path dependencies for cross-platform operations
pathdiff = "0.2"
colored = "3.1.1"
regex = "1.10"

# Configuration parsing
toml = "1.1"
md5 = "0.8"

# WAST test suite support
wast = "256.0"
wat = "1.244.0"

# Internal Kiln crates
kiln-error = { workspace = true }
kiln-foundation = { workspace = true }
kiln-format = { workspace = true }
kiln-decoder = { workspace = true }
kiln-runtime = { workspace = true }

# Optional testing framework integration
[dev-dependencies]
tempfile = "3.27"

[lints]
workspace = true
