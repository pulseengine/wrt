target/
docs/_build/
example/hello-world.wasm

# Ignore all WASM files but keep WAT files which are the source format
*.wasm
# Don't ignore WASM files in target when specifically built
!target/wasm32-*/*/example.wasm
