# Rustfmt configuration
build --@rules_rust//rust/settings:rustfmt.toml=//:rustfmt.toml

# Optional: Uncomment these lines to enable rustfmt check during builds
# build --aspects=@rules_rust//rust:defs.bzl%rustfmt_aspect
# build --output_groups=+rustfmt_checks

# Clippy configuration
# Note: To explicitly run clippy, use --aspects=@rules_rust//rust:defs.bzl%rust_clippy_aspect --output_groups=+clippy_checks
build --@rules_rust//rust/settings:clippy.toml=//:clippy.toml 